// Empty placeholder so the project compiles before the first aggregate
// (the refund saga, landing in M4.D) is added. Delete this file the
// moment Domain has real types.
namespace PayFlow.Reconciliation.Domain;
